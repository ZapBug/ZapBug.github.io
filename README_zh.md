# 我的个人主页

[English](./README.md) | [中文](./README_zh.md)

这是一个使用 Astro 构建并部署在 GitHub Pages 上的个人主页。

## 🚀 项目结构

在 Astro 项目中，你会看到以下文件夹和文件：

```text
/
├── public/
├── src/
│   ├── assets/           # 静态资源 (图片, 图标等)
│   ├── components/       # Astro 组件
│   ├── pages/            # 页面文件
│   │   └── index.astro   # 首页
│   ├── scripts/          # JavaScript 脚本
│   └── styles/           # CSS 样式文件
└── package.json
```

Astro 在 `src/pages/` 目录中查找页面文件（如 `.astro` 或 `.md` 文件）。每个页面根据其文件名作为路由暴露，例如 `src/pages/index.astro` 对应网站根路径。

`src/components/` 用于存放可重用的 Astro 组件。

`src/assets/` 用于存放静态资源，如图片和图标。

`public/` 目录用于存放需要直接访问的静态文件（如 favicon、robots.txt 等）。

## 🧞 命令

所有命令都从项目的根目录中的终端运行：

| 命令                      | 作用                                         |
| :------------------------ | :------------------------------------------- |
| `npm install`             | 安装依赖                                     |
| `npm run dev`             | 在 `localhost:4321` 启动本地开发服务器       |
| `npm run build`           | 构建生产站点到 `./dist/` 目录                |
| `npm run preview`         | 在部署前本地预览构建结果                     |
| `npm run astro ...`       | 运行 CLI 命令，如 `astro add`、`astro check` |
| `npm run astro -- --help` | 获取 Astro CLI 使用帮助                      |

## 📝 部署到 GitHub Pages

此项目配置为部署到 GitHub Pages。有关详细的工作流配置，请参阅 `.github/workflows/` 目录下的工作流文件。

## 许可证

本项目采用 MIT 许可证。

---

## 联系我

让我们一起学习，共同进步。

![微信公众号二维码](qrcode.png)
